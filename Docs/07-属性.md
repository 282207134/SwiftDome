# 07 · 属性

参考：[Swift 中文文档 · 属性](https://doc.swiftgg.team/documentation/the-swift-programming-language/properties)

## 学习目标

- 区分存储属性与计算属性
- 使用 `willSet` / `didSet` 属性观察器
- 掌握属性包装器 `@propertyWrapper`
- 理解类型属性 `static`

## 计算属性

```swift
struct Rect {
    var origin = Point()
    var size = Size()

    var center: Point {
        get {
            let centerX = origin.x + (size.width / 2)
            let centerY = origin.y + (size.height / 2)
            return Point(x: centerX, y: centerY)
        }
        set(newCenter) {
            origin.x = newCenter.x - (size.width / 2)
            origin.y = newCenter.y - (size.height / 2)
        }
    }
}
```

## 属性观察器

```swift
var totalSteps: Int = 0 {
    willSet(newTotalSteps) {
        print("即将设置为 \(newTotalSteps) 步")
    }
    didSet {
        if totalSteps > oldValue {
            print("增加了 \(totalSteps - oldValue) 步")
        }
    }
}
```

## 实战练习

1. **温度转换**：计算属性实现 Celsius ↔ Fahrenheit
2. **计步器**：属性观察器监控变化
3. **用户设置**：使用 `@propertyWrapper` 封装 UserDefaults

参见 `Sources/07-Properties/Properties.swift`

下一章 **《08 方法》**。